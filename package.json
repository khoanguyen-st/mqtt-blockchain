{
  "name": "mqtt-blockchain-poc",
  "version": "0.1.0",
  "description": "MQTT bridge PoC with Redis batching, hashing, and PostgreSQL storage.",
  "main": "src/index.js",
  "scripts": {
    "dev": "node src/index.js",
    "migrate": "node scripts/init-db.js",
    "publish:demo": "node scripts/publish.js --count=10 --interval=500"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "mqtt": "^5.10.1",
    "pg": "^8.11.5",
    "prom-client": "^15.1.3",
    "winston": "^3.13.0"
  }
}
